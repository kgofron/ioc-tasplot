# PyDevice (`src/`)

C++ device support for `DTYP="pydev"` records, vendored from
[PyDevice](https://github.com/klemenv/PyDevice) (same layout as ioc-hkl / ioc-pymca).

Upstream updates: replace files here or switch to an external `PYDEVICE` install in
`configure/RELEASE.local`.
