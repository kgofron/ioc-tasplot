# Optional template list for dbLoadTemplate tools.
# Runtime boot uses dbLoadRecords(..., "P=$(P),...") in st.cmd — MSI cannot
# expand $(P) inside this file (invalid character '$').
file "$(TOP)/plotApp/Db/plot.template"
{
pattern {P, MAXPTS, DATAFILETEXT}
{ TAS:Plot:, 4000, 65536 }
}
